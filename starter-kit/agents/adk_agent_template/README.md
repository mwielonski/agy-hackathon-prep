# Google ADK Agent Template

Use AGY to scaffold a new ADK agent into this directory:

```bash
agents-cli scaffold create --template default adk_agent_template
```

Or ask AGY in chat:
> *"AGY, scaffold a new Python ADK agent inside `starter-kit/agents/adk_agent_template` and add a BigQuery retriever tool."*
